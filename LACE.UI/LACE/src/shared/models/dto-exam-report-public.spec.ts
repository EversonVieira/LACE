import { DTOExamReportPublic } from './dto-exam-report-public';

describe('DTOExamReportPublic', () => {
  it('should create an instance', () => {
    expect(new DTOExamReportPublic()).toBeTruthy();
  });
});
